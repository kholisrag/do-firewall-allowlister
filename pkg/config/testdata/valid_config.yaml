log-level: DEBUG

cron:
  schedule: "0 0 * * *"
  timezone: "UTC"

digitalocean:
  api-key: "test-api-key"
  firewall-id: "test-firewall-id"
  inbound-rules:
    - port: 80
      protocol: tcp
    - port: 443
      protocol: tcp

netdata:
  domains:
    - "app.netdata.cloud"
    - "api.netdata.cloud"

cloudflare:
  ips-url: "https://api.cloudflare.com/client/v4/ips"
